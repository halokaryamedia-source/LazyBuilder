# Backlog

This file contains **future/non-active work only**. It never overrides `../next-action.md`.

## Current Backlog

1. **RQ-01 — Hunyuan local runtime setup (P1 after M1):** configure/test Hunyuan3D-2mv shape generation on RTX 3070 8 GB after the schematic→Axiom handoff path is proven.
2. **RQ-02 — Minecraftize full-block V0 (P1):** Blender mesh sampling, target width/scale, occupancy preview, and export after writer/handoff proof.
3. **RQ-03 — Stair fitting (P1 after V0):** 45° roof, facing, top/bottom, then inner/outer corner states from reproducible primitive tests.
4. **RQ-04 — Slab fitting (P1 after stairs):** top/bottom/half-height and mixed full/stair/slab primitive tests.
5. **RQ-05 — First real multi-view building (P1):** run end-to-end after V0/V1 primitives are stable.
6. **RQ-06 — Additional Minecraft block families (conditional):** wall/fence/glass pane/trapdoor/bars/doors only when a real build proves need.
7. **RQ-07 — MCP automation (deferred):** consider only after manual end-to-end production is useful and stable.
8. **RQ-08 — Advanced optimizer/ML/material systems (conditional):** only from measured quality/performance failure of simpler conversion.

## Not backlog by default

Other 3D providers are not standing migration tasks. Reopen provider choice only when current Hunyuan evidence demonstrates a material unresolved failure.
