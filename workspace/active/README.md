# Active Projects

Place local/external active LazyBuilder project packages under `workspace/active/<project>/` when convenient.

Project subdirectories are ignored by Git. This tracked README exists only to document the mount convention.
